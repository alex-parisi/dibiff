/// midi.h

#pragma once

namespace dibiff {
    /**
     * @brief MIDI Namespace
     * @details The MIDI namespace contains a collection of simple audio
     * MIDI objects that can be used to interact with MIDI devices.
     */
    namespace midi {
        class MidiInput;
    }
}