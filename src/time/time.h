/// time.h

#pragma once

namespace dibiff {
    /**
     * @brief Time Namespace
     * @details The delay namespace contains a collection of simple audio
     * effects that can be applied to an audio signal to affect its timing.
     */
    namespace time {
        class Delay;
    }
}